const expect = require('chai').expect;

beforeEach(function() {});

afterEach(function() {});

describe('the application starts', () => {
    it('has loaded the correct configuration', () => {});
});
